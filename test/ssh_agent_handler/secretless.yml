listeners:
  - name: sshagent
    protocol: ssh-agent
    socket: ./run/ssh-agent/.agent

handlers:
  - name: ssh-agent
    listener: sshagent
    debug: true
    credentials:
      - name: rsa
        provider: file
        id: /root/id_insecure
