version: 2

services:
  pg:
    protocol: pg
    listenOn: unix:///sock/.s.PGSQL.5432
    credentials:
      address: pg:5432
      username: myapp
      password:
        from: conjur
        get: dev:variable:db/password
