version: 2

services:
  template:
    connector: template
    listenOn: tcp://0.0.0.0:8080
    credentials:
      password: yourStrong()Password
      user: sa
      address: # address of the target

