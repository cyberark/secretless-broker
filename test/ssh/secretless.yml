listeners:
  - name: sshagent
    protocol: ssh-agent
    socket: ./run/ssh-agent/.agent

handlers:
  - name: ssh-agent
    listener: sshagent
    debug: true
    credentials:
      - name: rsa
        value:
          file: /root/id_insecure
