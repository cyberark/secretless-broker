apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: cyberark-sidecar-injector-webhook-deployment
  labels:
    app: cyberark-sidecar-injector
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cyberark-sidecar-injector
    spec:
      containers:
        - name: cyberark-sidecar-injector
          image: cyberark/sidecar-injector:test
          imagePullPolicy: Always
          args:
            - -tlsCertFile=/etc/webhook/certs/cert.pem
            - -tlsKeyFile=/etc/webhook/certs/key.pem
            - -alsologtostderr
            - -v=4
            - 2>&1
          volumeMounts:
            - name: webhook-certs
              mountPath: /etc/webhook/certs
              readOnly: true
      volumes:
        - name: webhook-certs
          secret:
            secretName: cyberark-sidecar-injector-webhook-certs
