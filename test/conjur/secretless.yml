listeners:
  - name: http_default
    protocol: http
    address: 0.0.0.0:80

handlers:
  - name: conjur
    listener: http_default
    debug: true
    match:
      - ^http\:\/\/conjur\/
    credentials:
      - name: accessToken
        provider: conjur
        id: accessToken
