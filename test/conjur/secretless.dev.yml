version: 2

services:
  conjur-http:
    protocol: http
    listenOn: tcp://0.0.0.0:1080
    credentials:
      accessToken:
        from: conjur
        get: accessToken
    config:
      authenticationStrategy: conjur
      authenticateURLsMatching:
        - ".*"
