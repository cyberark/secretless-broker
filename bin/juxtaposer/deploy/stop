#!/bin/bash
set -euo pipefail

kubectl delete deployment "${APP_NAME}" || true
kubectl delete serviceaccount "${APP_SERVICE_ACCOUNT}" || true
kubectl delete configmap ${APP_NAME}-secretless-config || true
kubectl delete configmap ${APP_NAME}-config || true
